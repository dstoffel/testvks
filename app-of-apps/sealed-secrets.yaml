apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: sealed-secrets
  namespace: ns1
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters: 
      selector:
        matchLabels:
          argocd/sealed: enable
  template:
    metadata:
      name: '{{.name}}-sealed-secrets'
    spec:
      project: "default"
      sources:
      - repoURL: https://github.com/dstoffel/testvks.git
        targetRevision: HEAD
        path: 'apps/sealed-secrets/'
      destination:
        name: '{{ .name }}'
        namespace: kube-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
