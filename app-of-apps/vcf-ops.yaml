apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vcf-ops
  namespace: test
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchLabels:
          argocd/sync-supervisor: "disable"
  template:
    metadata:
      name: '{{.name}}-vcf-ops'
      labels:
        app: vcf-ops
    spec:
      project: "default"
      sources:
      - repoURL: https://github.com/dstoffel/testvks.git
        targetRevision: HEAD
        path: 'apps/vcf-ops/'
      destination:
        name: '{{ .name }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true

